extends layout

block content
  .inner
    h2= title
    .map
      .autocomplete
        input.autocomplete__input(type='text' placehodler='Search for anything' name='geolocate')
      #map
        p Loading map...